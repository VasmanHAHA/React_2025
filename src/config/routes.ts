export const routNames = {
    home: '/home',
    redux: '/redux',
    mantine: '/mantine',
    tests: '/tests',
} as const

export const reduxRoutNames = {
    counters: 'counters',
    users: 'users',
} as const